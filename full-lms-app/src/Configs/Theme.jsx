import '@fontsource-variable/nunito-sans';
import { createTheme } from "@mui/material";

const Theme = createTheme({
    typography: {
        "fontFamily": `"Nunito Sans Variable", "Helvetica", "Arial", sans-serif`
    }
});

export default Theme;